# Backend Environment Example (sanitized)
# Copy to .env and fill values.

# Runtime
NODE_ENV=production
PORT=5000

# Database
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/<db-name>?retryWrites=true&w=majority

# Google Auth (if used by backend)
GOOGLE_CLIENT_ID=<your-google-oauth-client-id>
SHA1_Fingerprint=<your-android-keystore-sha1>

# CORS
# For local Expo packager during development (optional):
CORS_ORIGIN=http://localhost:8081
# For public testing; comma-separated list.
# Include your HTTPS tunnel (e.g., ngrok) and your Vercel app URL.
CORS_ORIGINS=https://<your-ngrok-subdomain>.ngrok.io,https://<your-vercel-app>.vercel.app

# Email Server (optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=<your-email>
EMAIL_PASS=<your-app-password>
EMAIL_SERVER_SECURE=false
EMAIL_FROM=<your-email>

# JWT
JWT_SECRET=<a-strong-random-secret>

# Cloudinary (optional)
CLOUDINARY_CLOUD_NAME=<cloud-name>
CLOUDINARY_API_KEY=<api-key>
CLOUDINARY_API_SECRET=<api-secret>
